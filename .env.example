# Copy to .env.local and fill in from your Supabase project settings.
# Leave both blank (or delete the file) to run in local mode: the game is fully
# playable, everything persists to localStorage, and no network calls are made.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
